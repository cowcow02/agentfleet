hub: wss://agentfleet-hub-production.up.railway.app
token: afm_charlie_001
machine_name: my-macbook

agents:
  - name: claude-dev
    description: "Claude Code for AgentFleet development — interactive in Ghostty"
    tags: [backend, frontend, feature, bug, refactor]
    capacity: 2
    invoke:
      launcher: "ghostty -e {script}"
      command: "claude 'You have been dispatched by AgentFleet to work on ticket {ticket_id}: {ticket_title}. {ticket_description}. Start by understanding the ticket and proposing your approach.' --dangerously-skip-permissions --dangerously-skip-permissions"
      workdir: /Users/cowcow02/Repo/kipwise/agentic-knowledge-system

  - name: scout
    description: "Quick codebase exploration — interactive in Ghostty"
    tags: [question, explore, simple]
    capacity: 2
    invoke:
      launcher: "ghostty -e {script}"
      command: "claude 'You have been dispatched by AgentFleet. Answer this question: {ticket_description}'"
      workdir: /Users/cowcow02/Repo/agentfleet

